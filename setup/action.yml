name: 'EasyP setup action'
description: 'setup EasyP tool'
inputs:
  version:
    description: 'Version of EasyP to install'
    required: false
    default: 'latest'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.version }}
branding:
  icon: 'package'
  color: 'blue'
